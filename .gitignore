# dependencies
node_modules
package-lock.json

# next.js build output
/.next/
/out/
/build

# typescript
*.tsbuildinfo
next-env.d.ts

# testing
/coverage

# asset-extraction scratch (sprite-id discovery dumps)
/tmp/
# diagnostic image dumps from scripts (review-clip, probe-anim-block, screenshot-ui, …)
scripts/tmp-*.png
# throwaway headless probes written against scripts/dev/harness.mjs
scripts/dev/tmp-*

# Claude Code: the skills, the agent and the shared allowlist are checked in.
# settings.local.json is not — it accumulates absolute paths from this machine.
.claude/settings.local.json
# AionUi symlinks these in from %APPDATA%; committing them stores an absolute
# path from one machine as the file's contents.
.claude/skills/cron
.claude/skills/officecli
.claude/skills/skill-creator

# editor
.vscode/

# env & local
.env*.local
.DS_Store
*.pem
